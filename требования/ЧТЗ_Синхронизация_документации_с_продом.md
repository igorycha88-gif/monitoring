# ЧТЗ: Синхронизация документации с реальным состоянием прода

**Дата:** 2026-08-22
**Маршрут:** 1 (Аналитик → Разработчик → Тестировщик → DevOps)
**Тип:** стандартное ЧТЗ (5 файлов)

## Контекст

После инцидента «не вижу сбора метрик на проде» (2026-08-22) диагностика
показала: сбор работает, но документация расходится с реальностью, что
затянуло диагностику и вводило в заблуждение:

1. Неверные имена env-переменных в `SKILL_DEVOPS_PROD.md`
   (`METRIKA_OAUTH_TOKEN` → реально `YANDEX_METRIKA_OAUTH_TOKEN`; не описаны
   персайтные `YANDEX_WEBMASTER_OAUTH_TOKENS`, `SITE_METRICS_API_KEYS`,
   `YANDEX_OAUTH_CLIENT_ID/SECRET`, `TELEGRAM_*`).
2. Порт **9093 (Alertmanager)** отсутствует в `AGENTS.md` и
   `SKILL_DEVOPS_PROD.md` (схема изоляции, pre-flight, проверки `ss`).
3. Не задокументированы volumes (`monitoring-db`, `prometheus-data`,
   `grafana-data`, `alertmanager-data`) и следствие: **свежий деплой = пустая
   SQLite-история Вебмастера → нужен backfill** (что и вызвало инцидент).
4. Не задокументированы диагностики из инцидента: нули Метрики при
   отсутствующем на сайте теге счётчика; рендер `monitoring_search_daily_*`
   только последнего завершённого дня (ADR-011); расписание Вебмастера
   07:00/19:00 МСК.
5. Историческое ЧТЗ содержит устаревшее имя `monitoring_ssl_cert_days_left`
   (реальное — `monitoring_ssl_days_left`); ЧТЗ не переписываются → нужны
   errata в живом документе.
6. Скрипт верификации прода живёт в `/tmp` сервера — должен быть в репо.

## Задачи

- **TASK-DOC-001.** `AGENTS.md`: добавить 9093 в раздел 0.1 и карту портов (§7).
- **TASK-DOC-002.** `ARCHITECTURE.md`: починить рамки ASCII-схемы; добавить
  раздел «Прод-справка»: volumes, cron-бэкап, расписания коллекторов,
  процедура post-deploy backfill, диагностика типовых «нет данных».
- **TASK-DOC-003.** `SKILL_DEVOPS_PROD.md`: исправить env-имена; добавить 9093
  (схема, РАЗРЕШЕНО, pre-flight, verify); таблица volumes; шаг «Backfill
  Вебмастера после деплоя» в процедуру; новые строки в «Типичные проблемы».
- **TASK-DOC-004.** Новый `PRODUCTION.md` (корень): единый runbook прода —
  актуальное состояние, порты, volumes, поток данных, расписания, секреты
  (только имена), процедуры (verify, backfill, backup/restore, откат),
  errata по именам метрик.
- **TASK-DOC-005.** Новый `scripts/verify_prod.py`: скрипт постфикс-верификации
  (коллекторы → Prometheus → Grafana через datasource proxy), запуск на VPS:
  `python3 scripts/verify_prod.py`; описать в PRODUCTION.md и SKILL_DEVOPS_PROD.md.

## Критерии приёмки

1. `pytest && ruff check . && ruff format --check . && mypy .` — зелёные
   (новый `scripts/verify_prod.py` проходит линтеры/типы).
2. `grep -R "METRIKA_OAUTH_TOKEN=" SKILL_DEVOPS_PROD.md` — пусто (только
   `YANDEX_METRIKA_OAUTH_TOKEN`); то же для `WEBMASTER_OAUTH_TOKEN=`.
3. Порт 9093 упомянут в AGENTS.md (карта портов) и SKILL_DEVOPS_PROD.md
   (pre-flight/verify).
4. Каждый из 5 документов содержит процедуру/ссылку на backfill Вебмастера
   после свежего деплоя (кроме AGENTS.md — там не требуется).
5. PRODUCTION.md содержит errata: реальное имя `monitoring_ssl_days_left`.
6. Исторические ЧТЗ/ADR не изменялись.
7. Секреты (значения токенов/паролей) не появились в коммитах.

## Файлы для изменения

- `AGENTS.md` (правка)
- `ARCHITECTURE.md` (правка)
- `SKILL_DEVOPS_PROD.md` (правка)
- `PRODUCTION.md` (новый)
- `scripts/verify_prod.py` (новый)

## Маршрутизация

**Архитектор:** не требуется (документация, архитектура не меняется).
**Исполнитель:** Разработчик → Тестировщик → DevOps (rsync на прод; пересборка
образа не обязательна для рантайма, но выполняется полной процедурой Правила 6).
